Simple object mapper to map data between 2 identical (or almost identical) shape objects.

## User Cases:
- Shallow cloning.
- Data archiving (moving data from the active table to the archived table).
